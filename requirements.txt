# Python package requirements for running dit_offload.py from source.
# Note: rclone is an external binary and is NOT installed via pip. See README for packaging instructions.

customtkinter>=6.0.0
pillow>=9.0.0
pyinstaller>=5.0
